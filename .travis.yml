language: java
notifications:
  irc:
    channels:
      - "irc.freenode.org#jnr"
    on_success: change
    on_failure: always
    template:
      - "%{repository} (%{branch}:%{commit} by %{author}): %{message} (%{build_url})"
matrix:
  include:
    - os: osx
    - os: linux
      jdk: oraclejdk8
    - os: linux
      jdk: openjdk7
    - os: linux-ppc64le
      jdk: openjdk8
sudo: false
cache:
  directories:
  - $HOME/.m2
